type: cli
allowNewFile: true
test: mocha
editable:
readonly:
  - test/**/basic/*.in
  - test/**/basic/*.out
  - test/*basic*
  - test/settings.json
  - test/judge.cpp  # judgeを使用するチャレンジ以外では削除
hide:
  - test/**/secret/*.in
  - test/**/secret/*.out
  - test/*secret*

templateType: stdin

# initialize: # judgeを使用するチャレンジ以外では削除
#  commands:
#    - g++ -std=c++11 -o test/judge test/judge.cpp
#    - rm test/judge.cpp
#  files:
#    - test/judge.cpp

images: # 画像を使用するチャレンジ以外では削除
  - img/*.png

testcases:
  open: 4
  secret: 4

evaluationPoint:
  "基本実装" : "条件判定の順序を適切に判断できる。"
  "Basic" : "No description."

languages: all
